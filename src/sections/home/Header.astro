---
import { Picture } from 'astro:assets';
import { Icon } from 'astro-icon/components';
import me from '~/assets/me.png';
import { Terminal } from '~/components/Terminal/Terminal';
import { CTALink } from '~/components/CTALink';
---

<header class="relative px-6 pt-8 pb-2 sm:px-8 sm:pt-10 sm:pb-4 md:px-6 md:pt-20 md:pb-6" data-no-print data-hero>
  <div class="max-w-section mx-auto flex w-full flex-col items-center gap-6 md:gap-14">
    <div class="flex flex-col items-center gap-6 text-center md:flex-row md:gap-12 md:text-left" data-testid="hero-intro">
      <figure class="hero-mark ring-panel-border-strong relative m-0 size-36 overflow-hidden rounded-full ring-1 md:size-44">
        <Picture
          src={me}
          alt="Chad Lefort"
          width={176}
          height={176}
          densities={[1, 2, 3]}
          loading="eager"
          fetchpriority="high"
          formats={['avif', 'webp']}
          fallbackFormat="webp"
          pictureAttributes={{ class: 'block size-full' }}
          class="size-full object-cover"
        />
        <div aria-hidden="true" class="bg-ink-300 pointer-events-none absolute inset-0 opacity-50 mix-blend-color">
        </div>
      </figure>
      <div class="hero-copy max-w-xl">
        <h1 class="font-display ml-0 text-6xl tracking-[-0.08em] sm:-ml-[8px] md:text-8xl" data-hero-name>
          Chad Lefort
        </h1>
        <p class="mt-3 text-xl text-accent font-medium md:text-2xl">Senior Frontend Engineer</p>
      </div>
    </div>

    <Terminal client:idle />

    <CTALink href="/chad-lefort-resume.pdf" download size="lg" className="hero-action" client:idle>
      <Icon
        name="lucide:download"
        class="size-5"
        aria-hidden="true"
      />
      Download Resume
    </CTALink>
  </div>
</header>
